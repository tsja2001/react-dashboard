import { memo } from 'react'
import style from './ConfigureChart.module.scss'

const ConfigureChart = () => {
  return (
    <div className={style.content}>
      <h2>ConfigureChart</h2>
    </div>
  )
}

export default memo(ConfigureChart)
